<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd" >
<mapper namespace="person">

  <select id="selectOne" parameterType="string" resultType="pdto">
  	select * from USER_TABLE where USER_ID=#{userId}
  </select>
  
  <!-- 아이디 중복 체크 -->
    <select id="idChk" parameterType="string" resultType="_int">
  	select count(USER_ID) from USER_TABLE WHERE USER_ID = #{userId}
  </select>

  <select id="selectList" resultType="pdto">
  	select * from USER_TABLE  
  </select>
  
  <!-- 개인회원 insert -->
  <insert id="insert" parameterType="pdto">
  	INSERT INTO USER_TABLE (USER_ID, USER_PW, USER_PHONE, USER_BIRTH, USER_EMAIL, ADRESS_ROAD, ADRESS_JIBUN, ADRESS_DETAIL, ADRESS_POSTCODE, USER_NAME, USER_CREATED_AT, USER_ACT, USER_ROLE, TERMS_ACT, TERMS_DATE, TERMS_PERIOD, USER_PIC)
	VALUES ( #{userId}, #{userPw}, #{userPhone}, #{userBirth}, #{userEmail}, #{AdressRoad},#{AdressJibun},#{AdressDetail}, #{AdressPostcode}, #{userName}, current_timestamp, 'true', 'ROLE_P', 'Y', current_timestamp, default, #{userPic})
  </insert>

	<!-- 개인회원 delete -->
  <delete id="delete" parameterType="pdto">
  	delete from USER_TABLE where USER_ID=#{userId}
  </delete>
  
  <!-- 개인회원 update -->	
  <update id="update" parameterType="pdto">
  	update USER_TABLE set 
  	  	USER_PW=#{userPw} ,
  	USER_PHONE=#{userPhone} ,
  	USER_EMAIL=#{userEmail} ,
  	TERMS_PERIOD=#{termsPeriod}
  	where USER_ID = #{userId}
  </update>
	

</mapper>
