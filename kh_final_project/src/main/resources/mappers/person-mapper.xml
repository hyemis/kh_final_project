<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd" >
<mapper namespace="person">

  <select id="selectOne" parameterType="string" resultType="pdto">
  	select * from USER_TABLE where USER_ID=#{USER_ID}
  </select>

  <select id="selectList" resultType="pdto">
  	select * from USER_TABLE  
  </select>
  
  <!-- 개인회원 insert -->
  <insert id="insert" parameterType="pdto">
<<<<<<< Updated upstream
  	INSERT INTO USER_TABLE (USER_ID, USER_PW, USER_PHONE, USER_BIRTH, USER_EMAIL, USER_ADRESS, USER_NAME, USER_ACT, USER_ROLE, TERMS_ACT, TERMS_DATE, TERMS_PERIOD)
VALUES ( #{userId}, #{userPw}, #{userPhone}, #{userBirth}, #{userEmail}, #{userAdress}, #{userName}, 'true', 'ROLE_P', 'Y', sysdate, default)
=======
  	insert into USER_TABLE  values( #{USER_ID}, #{USER_PW}, #{USER_PHONE}, #{USER_BIRTH}, #{USER_EMAIL}, #{USER_ADDRESS}, #{USER_NAME} )
>>>>>>> Stashed changes
  </insert>

  <delete id="delete" parameterType="pdto">
  	delete from USER_TABLE where USER_ID=#{USER_ID}
  </delete>
  	
  <update id="update" parameterType="pdto">
  	update USER_TABLE set 
  	USER_PASSWORD=#{USER_PASSWORD} 
  	USER_PHONE=#{USER_PHONE} 
  	USER_EMAIL=#{USER_EMAIL} 
  	USER_ADRESS=#{USER_ADRESS} 
  	TERMS_DATE=#{TERMS_DATE} 
  	where USER_ID = #{USER_ID}
  </update>
	
  <select id="accountmgr" resultType="pdto">
  	select * from USER_TABLE
  	WHERE USER_ROLE IN ('ROLE_A', 'ROLE_AM')
  </select>  
</mapper>
