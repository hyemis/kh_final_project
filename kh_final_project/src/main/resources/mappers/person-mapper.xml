<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd" >
<mapper namespace="person">

  <select id="selectOne" parameterType="string" resultType="pdto">
  	select * from USER_TABLE where USER_ID=#{userId}
  </select>
  
  <!-- 아이디 중복 체크 -->
    <select id="idChk" parameterType="string" resultType="_int">
  	select count(USER_ID) from USER_TABLE WHERE USER_ID = #{userId}
  </select>

  <select id="selectList" resultType="pdto">
  	select * from USER_TABLE  
  </select>
  
  <!-- 개인회원 insert -->
  <insert id="insert" parameterType="pdto">
  	INSERT INTO USER_TABLE (USER_ID, USER_PW, USER_PHONE, USER_BIRTH, USER_EMAIL, USER_ADRESS, USER_NAME, USER_ACT, USER_ROLE, TERMS_ACT, TERMS_DATE, TERMS_PERIOD, USER_PIC)
	VALUES ( #{userId}, #{userPw}, #{userPhone}, TO_DATE(#{userBirth}, 'yyyy-MM-dd'), #{userEmail}, #{userAdress}, #{userName}, 'true', 'ROLE_P', 'Y', current_timestamp, default, #{userPic})
  </insert>

  <delete id="delete" parameterType="pdto">
  	delete from USER_TABLE where USER_ID=#{USER_ID}
  </delete>
  	
  <update id="update" parameterType="pdto">
  	update USER_TABLE set 
  	USER_PASSWORD=#{USER_PASSWORD} 
  	USER_PHONE=#{USER_PHONE} 
  	USER_EMAIL=#{USER_EMAIL} 
  	USER_ADRESS=#{USER_ADRESS} 
  	TERMS_DATE=#{TERMS_DATE} 
  	where USER_ID = #{USER_ID}
  </update>
	

</mapper>
